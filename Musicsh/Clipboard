#!/bin/bash

mode="$(printf "Normal\nShuffle" | dmenu -f -i -p "Playing mode: ")"

if [ "$mode" = "Shuffle" ] ; then
	mpv --volume=60 --no-video --shuffle "$(xclip -selection clipboard -o)"
else
	mpv --volume=60 --no-video "$(xclip -selection clipboard -o)"
fi
